import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Find your series',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
